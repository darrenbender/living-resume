// ── BRAND ACCENT ────────────────────────────────────────────────────────
// A carrot-ish green that evokes the grocery-delivery space without
// reproducing any specific company's exact brand asset, logo, or wordmark.
// Approximate accent only — evoke, don't impersonate.
export const ACCENT = '#2f9e44' // active states, selected items, primary buttons
export const ACCENT_DARK = '#1f7a34' // hover / deeper accent
export const INK = '#0f172a' // structural dark (header, text, borders)
